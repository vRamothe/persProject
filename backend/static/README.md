# Static files directory
# CSS, JS, images, etc. served by Django

# CDN dependencies are loaded directly in base.html (Tailwind, HTMX, Alpine.js, KaTeX)
# Add project-specific static assets here if needed
